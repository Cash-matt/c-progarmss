#include<iostream>
#include<cmath>

int main()
{
  double x=3;
  double y=4;
  double z;

  // z=std::max(x,y); ////{Gives maximum value between x& y}/MAX
  //  z=std::min(x,y); ////{Gives minmum value between x& y} /MIN
  //  z=pow(2,3); ////{Gives power value of first number by addin power by seocnd num }//POWER 
  // z=sqrt(9); ////{Gives square root of number}//SQUARE ROOT
  //  z=abs(-6); ////{Gives oppsite of negaive number}
  // for more cmath functions
  //copy this link : https://cplusplus.com/reference/cmath/

  std::cout<<z;

return 0;
}